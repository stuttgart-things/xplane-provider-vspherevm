version: 2
builds:
  - main: ./cmd/provider
    binary: xplane-provider-vspherevm
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - formats:
      - tar.gz

changelog:
  use: github
  sort: asc
